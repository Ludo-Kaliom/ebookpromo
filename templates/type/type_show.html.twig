{% extends 'base.html.twig' %}

{% block title type.genre %}

{% block body %}
    <div class="container">
        <div class="row">
            <h1>Tous les livres disponibles dans de type {{ type.genre }}</h1>
            {{ include('partials/_menu.html.twig') }}
            {{ include('partials/_listbooks.html.twig') }}
        </div>
    </div>
    
{% endblock %}