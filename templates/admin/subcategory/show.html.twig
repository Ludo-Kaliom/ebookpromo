{% extends 'base_admin.html.twig' %}

{% block title %}Subcategory{% endblock %}

{% block body %}
    <h1>Subcategory</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ subcategory.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ subcategory.name }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ subcategory.status ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('subcategory_index') }}">back to list</a>

    <a href="{{ path('subcategory_edit', {'id': subcategory.id}) }}">edit</a>

    {{ include('admin/subcategory/_delete_form.html.twig') }}
{% endblock %}
